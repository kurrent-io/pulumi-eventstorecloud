name: kurrent-typescript
runtime: nodejs
description: Kurrent Cloud sample with TypeScript
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-west-1
